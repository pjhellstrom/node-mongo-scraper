<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">TechCrunch Scraper</a>
    </div>
    <ul class="nav navbar-nav navbar-right top-buttons">
      <li><button class="btn btn-primary home">Back to Homepage</button></li>
    </ul>
  </div>
</nav>

<div class="articles">
  {{#each articles}}
  {{#if saved}}
  <div class="article">
    <a href="{{link}}">
      <h2>{{title}}</h2>
    </a>
    <p>{{summary}}</p>
    <button class="btn btn-danger delete" data-id="{{_id}}">Delete</button>
    {{!-- <button class="btn btn-primary addNote" data-id="{{_id}}">NOTE</button> --}}
    <br>
  </div>
  {{/if}}
  {{/each}}
</div>
